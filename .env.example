# Base URL for the backend API (used once a backend is connected)
VITE_API_BASE_URL=http://localhost:4000/api

# Exchange rate API (used by GlobalStateContext for live currency conversion)
VITE_EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest/CHF
